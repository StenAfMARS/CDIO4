package Fields;

import java.awt.*;

class ModelStart extends ModelField{
 public ModelStart(String title, String subtext, String description, Color backgroundColor) {
  super(title, subtext, description, backgroundColor);
 }
}
