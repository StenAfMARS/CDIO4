package Fields;

import java.awt.*;

class ModelStart extends ModelField{
 /**
  * ModelStart used to make a model for the field START.
  * @param name name of the field.
  * @param backgroundColor background color of the field.
  */
 public ModelStart(String name, Color backgroundColor) {
  super(name, backgroundColor);
 }
}
