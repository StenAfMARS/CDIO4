package Fields;

import java.awt.*;

class ModelVisitJail extends ModelField {
 /**
  * ModelVisitJail used to make a model for the field Visit Jail.
  * @param name name of the field.
  * @param backgroundColor background color of the field.
  */
 public ModelVisitJail(String name, Color backgroundColor) {
  super(name, backgroundColor);
 }
}
